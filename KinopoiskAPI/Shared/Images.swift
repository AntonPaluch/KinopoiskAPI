import UIKit

enum Images {
    enum tabBar {
        static let movie = UIImage(named: "MovieTabIcon")!
        static let search = UIImage(named: "SearchTabIcon")!
    }
    enum placeholder {
        static let placeholderFilms = UIImage(named: "placeholderFilms")!
    }
}
